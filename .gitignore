# 비공개 키 — 절대 커밋 금지
keys.json

# === 개인 데이터 — 절대 커밋 금지 (NAS/로컬에만 보관) ===
# 공개 레포라 여기 들어가면 전 세계 노출됨. 후기·사진·별점·블로그 초안 일체 차단.
posts/
drafts/
reviews.json
review*.json
# 사진(아이 얼굴·EXIF GPS 등) — 사이트는 외부 이미지 URL만 쓰므로 로컬 이미지는 전부 제외
*.jpg
*.jpeg
*.png
*.heic
*.heif
*.webp
*.gif
*.bmp
*.tiff
*.mov
*.mp4

# 생성물
public/

# 파이썬/맥 부산물
__pycache__/
*.pyc
.DS_Store

# === 비공개 백엔드·로컬 설정 — 공개 repo 노출 절대 금지 ===
# 평가/블로그 백엔드(평가·휴게소 코드, D1/R2 바인딩, 시크릿 참조), 로컬 Claude 설정/스킬/훅,
# 인수인계 문서(계정ID·암호·키 포함), 데이터 백업. git add -A 사고로도 안 새게 명시 차단.
cloudflare/
.claude/
HANDOFF.md
*.bak
.wrangler/
